# Four-agent landing-page experiment

This repository is intentionally reset to a clean baseline for a coordinated
four-agent website build. Each agent will work in its own isolated clone and
Git branch; changes will be reviewed and merged into `main` after the test.
